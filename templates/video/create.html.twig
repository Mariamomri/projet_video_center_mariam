{% extends 'base.html.twig' %}

{% block title %}Ajout d'une vidéo{% endblock %}

{% block body %}
    <h1>Ajout d'une vidéo</h1>

    {{ form_start(monForm) }}
        {{ form_row(monForm.title) }}
        {{ form_row(monForm.videoLink) }}
        {{ form_row(monForm.descritpion) }}
        {{ form_row(monForm.save) }}
    {{ form_end(monForm) }}

    <a href="{{ path('app_home') }}">Retour</a>
{% endblock %}